<%= form_for [@recipe, @recipe.comments.build] do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :author, "Author" %><br>
    <%= f.text_field :author %>
  </p>
  <p><%= f.text_area :body, :rows => 4 %></p>
  <p><%= f.submit %></p>
<% end %>
