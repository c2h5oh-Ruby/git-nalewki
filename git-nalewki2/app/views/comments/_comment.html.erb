<p><span style="color:#FF0000; padding-left:5px;"><%= comment.author %></span> napisał <span style="color:#FF0000; padding-left:5px; font-weight:bold;">"<%= comment.body %>"</span> 
<span style="padding-left:30px">
<% if user_signed_in? %>
<%= link_to "Usun komentarz", [comment.recipe, comment], :confirm => "Na pewno?", :method => :delete %></span><p>
<% end %> 