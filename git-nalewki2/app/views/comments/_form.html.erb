<%= form_for [@recipe, @recipe.comments.build] do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :author, "Autor:" %><%= f.text_field :author %>
	<span style="padding-left:10px"></span>Treść: <%= f.text_area :body, :rows => 1 %>
	<%= f.submit 'Publikuj' %>
  </p>
<% end %>
